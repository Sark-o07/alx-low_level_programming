#include "main.h"

/**
 * mul - a function that multiplies two integers.
 *@a: the first integer
 *@b: the second integer.
 *Return: the product of a and b.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
